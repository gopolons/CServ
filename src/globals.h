#ifndef Globals_h
#define Globals_h

/*
 * Variable used by receiver and argument handler
 * determining the port on which the stream socket
 * is going to be listening for incoming connections.
 */
extern char port[5];

#endif // !Globals_h
